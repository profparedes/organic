import styled from 'styled-components';

export const BorderQuantity = styled.div`
  max-width: 50%;
  border: 1px solid var(--primary-color);
`;

export const CloseButton = styled.div`
  color: var(--primary-color);

  &:hover {
    color: red;
  }
`;
